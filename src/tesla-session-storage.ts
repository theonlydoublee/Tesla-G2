/** Bridge localStorage key: server session UUID (tokens live on server only). */
export const STORAGE_KEY_SESSION_ID = 'tesla_session_id';

/** Mirror of server session region (e.g. eu, na) for debugging/UI. */
export const STORAGE_KEY_FLEET_REGION = 'tesla_fleet_region';

/** Mirror of resolved Fleet API origin for this session. */
export const STORAGE_KEY_FLEET_API_BASE = 'tesla_fleet_api_base';
